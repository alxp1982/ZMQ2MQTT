name: zmq2mqtt pull request

on:
  pull_request:
    types: [opened, reopened]

jobs:
  build:

    runs-on: ubuntu-latest
    steps:
      - uses: mshick/add-pr-comment@v2
        with:
          message: |
            **Hello**
            🌏
            !