name: zmq2mqtt pull request

on:
  pull_request:
    types: [opened, reopened]

jobs:
  build:
    runs-on: ubuntu-latest
    permissions: 
      pull-requests: write 
    steps:
      - name: Go Coverage Diff Action
      # You may pin to the exact commit or the version.
      # uses: PanagiotisPtr/cov-diff@b98299bcbfb64ad95cd768a5ccafd15a00c72244
        uses: PanagiotisPtr/cov-diff@v1
            
